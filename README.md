## jenkin-pipeline-2

### simple-node-ci

Tiny Node app to demo Jenkins pipeline.

- `npm install` to install dev deps (mocha, reporter)
- `npm test` runs unit tests and produces test-results/results.xml
- `npm run build` creates dist/app.js
- Dockerfile builds an image that prints greeting and exits
